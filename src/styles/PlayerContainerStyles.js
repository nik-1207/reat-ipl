import { createUseStyles } from "react-jss";

const TEAM_CONTAINER = createUseStyles({
  teamContainer: {
    display: "flex",
    flexWrap: "wrap",
    justifyContent: "center",
  },
});
export default TEAM_CONTAINER;
